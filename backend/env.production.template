# Production Environment Variables for MatBao Hosting
# Copy this file to .env on the production server

NODE_ENV=production
PORT=3000
# Note: MatBao hosting doesn't allow port 5000. Use port 3000, 8080, or let MatBao assign a port.
# If MatBao assigns a port automatically, you can omit PORT and it will use the assigned port via environment variable.
FRONTEND_URL=https://wam.vuleits.com

# Database Configuration - Primary (Remote Hosting Database)
# The system will automatically construct DATABASE_URL from these variables
DB_HOST=172.236.137.93
DB_PORT=3306
DB_NAME=vul19326_wam
DB_USER=vul19326_wamadmin
DB_PASSWORD=Wamdmin@2025

# Database Configuration - Fallback (Optional)
# If primary database connection fails, system will automatically try fallback
# Configure DB2_* variables for fallback database connection
# DB2_HOST=172.236.137.94
# DB2_PORT=3306
# DB2_NAME=vul19326_wam_backup
# DB2_USER=vul19326_wamadmin
# DB2_PASSWORD=Wamdmin@2025

# Alternative: Direct DATABASE_URL (will be auto-constructed from DB_* if not set)
# DATABASE_URL="mysql://vul19326_wamadmin:Wamdmin%402025@172.236.137.93:3306/vul19326_wam"

# Session Secret (generate a random string for production)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production
